# Backend server
HOST=0.0.0.0
PORT=8000

# Frontend directory to serve static files (index.html and dashboard.jsx)
FRONTEND_DIR=/workspace/frontend

# Uploads directory for CSV files
UPLOAD_DIR=/workspace/backend/uploads

# Auth config
JWT_SECRET=change-me
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
PASSWORD_SALT=change-this-too

# Optional AI config
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Security
ALLOWED_ORIGINS=*
